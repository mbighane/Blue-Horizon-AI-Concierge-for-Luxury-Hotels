"""Unit tests for OpenAIChatAgent — SKIPPED (agent removed, replaced by Concierge Agent)."""
import pytest

pytestmark = pytest.mark.skip(reason="OpenAIChatAgent was removed; Concierge Agent is the sole agent.")

